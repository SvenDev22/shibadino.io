import RotatingSection from "../_components/08-rotation-animation";

export default function FooterSection() {
  return (
    <section
      className="flex h-fit w-full flex-col items-center justify-start"
      id="footer"
    >
      <RotatingSection />
    </section>
  );
}
